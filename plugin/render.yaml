services:
  - type: web
    name: mcp-control-hub-plugin
    runtime: node
    rootDir: plugin
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
    healthCheckPath: /health
